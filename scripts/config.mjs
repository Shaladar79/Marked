// scripts/config.mjs

export const MarkedConfig = {};

// Example core attributes — adjust once you lock them in.
MarkedConfig.attributes = {
  body: { label: "Body" },
  mind: { label: "Mind" },
  soul: { label: "Soul" }
};

// You can add skills, ranks, marks, etc. here later:
// MarkedConfig.ranks = { quartz: { label: "Quartz" }, ... }

