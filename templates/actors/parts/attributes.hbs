{{!-- templates/actors/parts/attributes.hbs --}}

<section class="attributes-section">

  <h2 class="block-title">Attributes</h2>

  <div class="attribute-grid">

    {{!-- BODY COLUMN --}}
    <div class="attribute-group">
      <header class="attribute-group-header">
        {{system.attributes.body.label}}
      </header>

      <div class="attr-row primary-attr">
        <label>Body Total</label>
        <input type="number"
               name="system.attributes.body.value"
               data-dtype="Number"
               value="{{system.attributes.body.value}}" />
      </div>

      <div class="attr-row">
        <label>{{system.attributes.body.might.label}}</label>
        <input type="number"
               name="system.attributes.body.might.value"
               data-dtype="Number"
               value="{{system.attributes.body.might.value}}" />
      </div>

      <div class="attr-row">
        <label>{{system.attributes.body.swiftness.label}}</label>
        <input type="number"
               name="system.attributes.body.swiftness.value"
               data-dtype="Number"
               value="{{system.attributes.body.swiftness.value}}" />
      </div>

      <div class="attr-row">
        <label>{{system.attributes.body.fortitude.label}}</label>
        <input type="number"
               name="system.attributes.body.fortitude.value"
               data-dtype="Number"
               value="{{system.attributes.body.fortitude.value}}" />
      </div>

      <div class="attr-row">
        <label>{{system.attributes.body.endurance.label}}</label>
        <input type="number"
               name="system.attributes.body.endurance.value"
               data-dtype="Number"
               value="{{system.attributes.body.endurance.value}}" />
      </div>
    </div>

    {{!-- MIND COLUMN --}}
    <div class="attribute-group">
      <header class="attribute-group-header">
        {{system.attributes.mind.label}}
      </header>

      <div class="attr-row primary-attr">
        <label>Mind Total</label>
        <input type="number"
               name="system.attributes.mind.value"
               data-dtype="Number"
               value="{{system.attributes.mind.value}}" />
      </div>

      <div class="attr-row">
        <label>{{system.attributes.mind.insight.label}}</label>
        <input type="number"
               name="system.attributes.mind.insight.value"
               data-dtype="Number"
               value="{{system.attributes.mind.insight.value}}" />
      </div>

      <div class="attr-row">
        <label>{{system.attributes.mind.quickness.label}}</label>
        <input type="number"
               name="system.attributes.mind.quickness.value"
               data-dtype="Number"
               value="{{system.attributes.mind.quickness.value}}" />
      </div>

      <div class="attr-row">
        <label>{{system.attributes.mind.willpower.label}}</label>
        <input type="number"
               name="system.attributes.mind.willpower.value"
               data-dtype="Number"
               value="{{system.attributes.mind.willpower.value}}" />
      </div>

      <div class="attr-row">
        <label>{{system.attributes.mind.focus.label}}</label>
        <input type="number"
               name="system.attributes.mind.focus.value"
               data-dtype="Number"
               value="{{system.attributes.mind.focus.value}}" />
      </div>
    </div>

    {{!-- SOUL COLUMN --}}
    <div class="attribute-group">
      <header class="attribute-group-header">
        {{system.attributes.soul.label}}
      </header>

      <div class="attr-row primary-attr">
        <label>Soul Total</label>
        <input type="number"
               name="system.attributes.soul.value"
               data-dtype="Number"
               value="{{system.attributes.soul.value}}" />
      </div>

      <div class="attr-row">
        <label>{{system.attributes.soul.presence.label}}</label>
        <input type="number"
               name="system.attributes.soul.presence.value"
               data-dtype="Number"
               value="{{system.attributes.soul.presence.value}}" />
      </div>

      <div class="attr-row">
        <label>{{system.attributes.soul.grace.label}}</label>
        <input type="number"
               name="system.attributes.soul.grace.value"
               data-dtype="Number"
               value="{{system.attributes.soul.grace.value}}" />
      </div>

      <div class="attr-row">
        <label>{{system.attributes.soul.resolve.label}}</label>
        <input type="number"
               name="system.attributes.soul.resolve.value"
               data-dtype="Number"
               value="{{system.attributes.soul.resolve.value}}" />
      </div>

      <div class="attr-row">
        <label>{{system.attributes.soul.resonance.label}}</label>
        <input type="number"
               name="system.attributes.soul.resonance.value"
               data-dtype="Number"
               value="{{system.attributes.soul.resonance.value}}" />
      </div>
    </div>

  </div>
</section>

